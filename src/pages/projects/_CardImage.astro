---
import { Image } from "astro:assets";

interface Props {
  src: ImageMetadata;
  alt: string;
  priority?: boolean;
}
const { src, alt, priority } = Astro.props;
if (src.format != "svg") {
  throw new Error("must be svg");
}
---

<Image {src} {alt} {priority} width={30 * 16} />
